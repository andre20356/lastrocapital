kind = "web"
previewPath = "/"
title = "LastroCapital"
version = "1.0.0"
id = "artifacts/lastrocapital"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20844

[services.development]
run = "pnpm --filter @workspace/lastrocapital run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/lastrocapital", "run", "build" ]
publicDir = "artifacts/lastrocapital/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20844"
BASE_PATH = "/"
